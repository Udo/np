new l = (: name = 'King Joffrey' occupation = 'Tyrant' status = 'poisoned' 1 2 3 4 )

events(l, (: 
  event = {(tbl idx)
    print('accessing' idx)
    = {(a b c d e)
      print('func' idx a b c d e)
			= a
    }
  }
	blr = {(meTable) print('BLR: ' meTable.name) }
))

l:bla(10 11 12)

l:blr(20 21 22 23 24)

print(("abcd"):upper())

new arbiray = (: 1 2 3 4 5)

print(arbiray:join('-'))

print('arbiray size ' arbiray:count())

new name, occupation, status in l;

print(name occupation status)

for k,v in each(lmpf) {
  print(k v)
}

-- each() with an empty object should just not do anything
for k,v in each(lmpf) {
  print(k v)
}

