new memoize = { fn | 
  new memList = (:)
  => { n | 
    if(memList[n]) => memList[n]
    else => fn(n this)
    }
  }
  
-- fast fibonacci generator
new memFib = memoize{ n f |
  if(n < 3) => n
  else => f(n-1 f) + f(n-2 f)
	}
  
print( memFib(20) )