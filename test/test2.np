new a = 1

new fooFunc = {(bar)
  if bar > 0 {
    return('greater than zero')
  else bar < 0
    return('less than zero')
  else true
    return('equal to zero')
  }
  return(bar)
}

print(fooFunc(-10))