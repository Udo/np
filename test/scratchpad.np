
new cr = coroutine.create{ x |
  while(true) {
		x += 1
		coroutine.yield(x)	  
		}
  }
	
--list.each(bind(cr) { v k | print(k)})


print(#status cr:status())
print(cr:resume(10))
print(cr:resume(20))
print(cr:resume(30))