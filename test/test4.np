new myBehavior = (: 
  test = {(ctx str) =str }
  add = list.add
	create = {(obj) obj.name = 'testname'; print('CREATE'); =obj }
	);

--create()

new rl = create(myBehavior, (: 1 2 3 4))
rl += 1
--events(rl, myBehavior)

print('content -------------')
for k,v in each(rl) {
	print(k v)
}

print('test call ' rl:test('abc'))